\usepackage{xcolor}
\usepackage{xstring}

\definecolor{AMaubergine}{rgb}{0.2,0.18,0.22}
\definecolor{AMorange}{rgb}{0.84,0.32,0.28}

% Redefine these colors to your liking in the preamble.
\definecolor{TitleColor}{named}{AMorange}
\definecolor{InnerLinkColor}{named}{AMorange}
\definecolor{OuterLinkColor}{named}{AMorange}
% Redefine these colors to something not white if you want to have colored
% background and border for code examples.
%\definecolor{VerbatimColor}{rgb}{0.92,0.92,0.92}%{0.99,0.77,0.65}
%\definecolor{VerbatimBorderColor}{named}{AMaubergine}

%\usepackage{tikz}
%\usetikzlibrary{backgrounds}

% TODO: change the colours to fit the emul8 style better
% TODO: get textbf as topic title, example here: http://tex.stackexchange.com/questions/9347/how-to-define-a-new-environment-with-a-fancy-box-tikz-around-sample-source-cod
% TODO: fully colored background? (have to get rid of verbatim's bg)

%\makeatletter
%\newenvironment{topicbox}
%{
%    \setbox\@tempboxa=\hbox\bgroup\color@setgroup
%    \begin{minipage}{0.95\linewidth}
%}
%{
%    \end{minipage}
%    \color@endgroup\egroup
%    \begin{tikzpicture}
%        \node[tbox] (box)
%            {\makebox[.95\linewidth][l]{\box\@tempboxa}};
%    \end{tikzpicture}
%}
%\makeatother

%\tikzstyle{tbox} = [
%    top color=AMaubergine!40,
%    bottom color=AMaubergine!60,
%    draw=AMaubergine, thick, rectangle, rounded corners,
%    inner sep=10pt
%]

\makeatletter 
  \fancypagestyle{normal}{
    \fancyhf{}
    \headheight 40pt
    \setlength{\topmargin}{-20mm}
    \fancyfoot[LE,RO]{{\py@HeaderFamily\thepage}}
    \fancyfoot[LO,RE]{{\py@HeaderFamily \@title}}
    \fancyhead[LO]{{\py@HeaderFamily\nouppercase{\rightmark}}}
    \fancyhead[RE]{{\py@HeaderFamily\nouppercase{\leftmark}}}
    \fancyhead[LE,RO]{{\includegraphics[height=25pt]{emul8-logo.pdf}}}
    \renewcommand{\headrulewidth}{0.4pt}
    \renewcommand{\footrulewidth}{0.4pt}
  }
\makeatother

\PassOptionsToClass{openany}{\sphinxdocclass}



\let\oldcaption\caption
\renewcommand{\caption}[1]{
	\IfSubStr{#1}{(T)}{\oldcaption{#1}\tiny}{\oldcaption{#1}}
}


